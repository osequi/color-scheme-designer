import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorTune from ".";

/**
 * Displays the ColorTune demo.
 */
const ColorTuneDemo = (props) => {
  return <ColorTune />;
};

export default ColorTuneDemo;
