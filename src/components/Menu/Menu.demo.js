import React from "react";

/**
 * Imports other components and hooks.
 */
import Menu from ".";

/**
 * Displays the Menu demo.
 */
const MenuDemo = (props) => {
  return <Menu />;
};

export default MenuDemo;
