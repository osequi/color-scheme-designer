import React from "react";
import ColorTint from "./ColorTint";

export default {
  component: ColorTint,
  title: "ColorTint",
};

const Template = (args) => <ColorTint {...args} />;

export const Default = Template.bind({});
Default.args = {};
