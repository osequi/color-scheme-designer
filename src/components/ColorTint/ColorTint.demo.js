import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorTint from ".";

/**
 * Displays the ColorTint demo.
 */
const ColorTintDemo = (props) => {
  return <ColorTint />;
};

export default ColorTintDemo;
