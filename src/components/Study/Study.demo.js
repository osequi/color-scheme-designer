import React from "react";

/**
 * Imports other components and hooks.
 */
import Study from ".";

/**
 * Displays the Study demo.
 */
const StudyDemo = (props) => {
  return <Study />;
};

export default StudyDemo;
