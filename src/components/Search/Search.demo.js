import React from "react";

/**
 * Imports other components and hooks.
 */
import Search from ".";

/**
 * Displays the Search demo.
 */
const SearchDemo = (props) => {
  return <Search />;
};

export default SearchDemo;
