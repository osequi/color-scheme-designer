import React from "react";

/**
 * Imports other components and hooks.
 */
import Add from ".";

/**
 * Displays the Add demo.
 */
const AddDemo = (props) => {
  return <Add />;
};

export default AddDemo;
