import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorInfo from ".";

/**
 * Displays the ColorInfo demo.
 */
const ColorInfoDemo = (props) => {
  return <ColorInfo />;
};

export default ColorInfoDemo;
