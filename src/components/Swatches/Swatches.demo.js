import React from "react";

/**
 * Imports other components and hooks.
 */
import Swatches from ".";

/**
 * Displays the Swatches demo.
 */
const SwatchesDemo = (props) => {
  return <Swatches />;
};

export default SwatchesDemo;
