import React from "react";

/**
 * Imports other components and hooks.
 */
import Presenter from ".";

/**
 * Displays the Presenter demo.
 */
const PresenterDemo = (props) => {
  return <Presenter />;
};

export default PresenterDemo;
