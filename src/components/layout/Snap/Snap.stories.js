import React from "react";
import Snap from "./Snap";

export default {
  component: Snap,
  title: "Snap",
};

const Template = (args) => <Snap {...args} />;

export const Default = Template.bind({});
Default.args = {};
