import React from "react";

/**
 * Imports other components and hooks.
 */
import Snap from ".";

/**
 * Displays the Snap demo.
 */
const SnapDemo = (props) => {
  return <Snap />;
};

export default SnapDemo;
