import React from "react";

/**
 * Imports other components and hooks.
 */
import Designer from ".";

/**
 * Displays the Designer demo.
 */
const DesignerDemo = (props) => {
  return <Designer />;
};

export default DesignerDemo;
