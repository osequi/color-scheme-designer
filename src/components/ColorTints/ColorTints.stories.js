import React from "react";
import ColorTints from "./ColorTints";

export default {
  component: ColorTints,
  title: "ColorTints",
};

const Template = (args) => <ColorTints {...args} />;

export const Default = Template.bind({});
Default.args = {};
