import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorTints from ".";

/**
 * Displays the ColorTints demo.
 */
const ColorTintsDemo = (props) => {
  return <ColorTints />;
};

export default ColorTintsDemo;
