import React from "react";

/**
 * Imports other components and hooks.
 */
import Color from ".";

/**
 * Displays the Color demo.
 */
const ColorDemo = (props) => {
  return <Color />;
};

export default ColorDemo;
