import React from "react";

/**
 * Imports other components and hooks.
 */
import Text from ".";

/**
 * Displays the Text demo.
 */
const TextDemo = (props) => {
  return (
    <Text variant="longform">
      Use MDX with CNA. `useMarkdown` demos removed.
    </Text>
  );
};

export default TextDemo;
