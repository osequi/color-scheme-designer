import React from "react";

/**
 * Imports other components and hooks.
 */
import ColorPair from ".";

/**
 * Displays the ColorPair demo.
 */
const ColorPairDemo = (props) => {
  return <ColorPair />;
};

export default ColorPairDemo;
